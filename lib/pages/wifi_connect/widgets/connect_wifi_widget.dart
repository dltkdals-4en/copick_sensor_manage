import 'package:flutter/material.dart';

class ConnectWifiWidget extends StatelessWidget {
  const ConnectWifiWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(
        child: Column(
          
        ),
      ),
    );
  }
}
